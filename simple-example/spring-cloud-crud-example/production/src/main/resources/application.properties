spring.application.name=user-service
spring.profiles.active=dev

server.port=9902



eureka.instance.prefer-ip-address=true

#服务过期时间配置,超过这个时间没有接收到心跳EurekaServer就会将这个实例剔除
#注意，EurekaServer一定要设置eureka.server.eviction-interval-timer-in-ms否则这个配置无效，这个配置一般为服务刷新时间配置的三倍
#默认90s
eureka.instance.lease-expiration-duration-in-seconds=15
#服务刷新时间配置，每隔这个时间会主动心跳一次
#默认30s
eureka.instance.lease-renewal-interval-in-seconds=5

#eureka client刷新本地缓存时间
#默认30s
eureka.client.registryFetchIntervalSeconds=5
#eureka客户端ribbon刷新时间
#默认30s
ribbon.ServerListRefreshInterval=5000

eureka.client.service-url.defaultZone=http://localhost:9901/eureka


spring.datasource.password= a12345
spring.datasource.username= huifer
spring.datasource.driver-class-name= com.mysql.cj.jdbc.Driver
spring.datasource.url= jdbc:mysql://47.98.225.144:3306/dest?useUnicode=true&characterEncoding=utf8&useSSL=false

spring.redis.database=2